# Active

Tareas en ejecución, movidas desde `tasks/backlog/` al iniciar el trabajo. Una tarea sale de aquí solo hacia `tasks/completed/` vía `/task-close` (verificación de acceptance criteria, tests, build, review, scope y documentación) o de vuelta a `tasks/backlog/` si se pausa.
