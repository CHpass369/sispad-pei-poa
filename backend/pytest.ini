[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
testpaths = tests apps
# Suite paralela: un worker por CPU, agrupando por módulo (evita contención
# de BD de test entre módulos). Desactivar con: -p no:xdist -n 0
addopts = -n auto --dist loadscope
